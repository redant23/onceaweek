import styled from 'styled-components';

const Count = styled.div`

  position: fixed;
  top: 10px;
  left: 10px;

  font-size: 60px;
  font-weight: 900;
  color: #646464;

  text-shadow:
    -1px -1px 0 #fff,
    1px -1px 0 #fff,
    -1px 1px 0 #fff,
    1px 1px 0 #fff;

`;

export default Count;